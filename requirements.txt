Django>=5.2,<5.3
djangorestframework>=3.15
django-filter>=24.3
psycopg2-binary>=2.9
Pillow>=10.4
openpyxl>=3.1
reportlab>=4.2
python-dotenv>=1.0
redis>=5.0
pyTelegramBotAPI>=4.20

# --- Serverga joylashtirish (Render, VPS) ---
gunicorn>=22.0        # ishlab chiqarish uchun veb-server
# Statik fayllarni Django o'zi bersin (Nginx yo'q joyda).
# [brotli] - CSS/JS ni gzip'dan ham ~20% kichikroq qilib siqadi. Sekin
# mobil internetda bu bir necha yuz millisekund tejaydi va hech qanday
# kod o'zgarishi talab qilmaydi: WhiteNoise mos brauzerga o'zi beradi.
whitenoise[brotli]>=6.7
